#spring.application.name=revenue-service

server:
  port: 8087

spring:
  application:
    name: revenue-service
  datasource:
    url: jdbc:mysql://localhost:3306/revenue_db
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

jwt:
  secret: staynest-secret-key-for-jwt-signing-2026
  expiration: 86400000